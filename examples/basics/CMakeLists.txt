# Getting started: declare a schema, resolve inputs, read back, and observe.
nucleus_add_example(quickstart nucleus::argv nucleus::runtime)
nucleus_add_example(diagnostics)
nucleus_add_example(logging nucleus::argv)
