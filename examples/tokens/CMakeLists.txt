# Token expansion: core tokens, a host-authored tokenizer category, and the
# auto-named pkey tokenizer.
nucleus_add_example(tokens nucleus::env)
nucleus_add_example(time_tokenizer nucleus::env)
nucleus_add_example(pkey_tokenizer nucleus::runtime)
