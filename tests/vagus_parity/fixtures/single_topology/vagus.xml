<?xml version="1.0"?>
<vagus>
    <node name="yin">
        <logger log_level="info"/>
        <configuration name="demo">
            <message>
                <greeting>hello from yin (single_topology layout)</greeting>
                <description>one XML lists every node; each binary picks its block via --vagus-node-name</description>
            </message>
        </configuration>
    </node>
    <node name="yang">
        <logger log_level="info"/>
        <configuration name="demo">
            <message>
                <greeting>hello from yang (single_topology layout)</greeting>
                <description>one XML lists every node; each binary picks its block via --vagus-node-name</description>
            </message>
        </configuration>
    </node>
</vagus>
