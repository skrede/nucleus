<?xml version="1.0"?>
<vagus>
    <node name="yin">
        <logger log_level="info"/>
        <configuration name="demo">
            <message>
                <greeting>hello from yin (per_node_files layout)</greeting>
                <description>each binary loads its own per-node XML; no shared base</description>
            </message>
        </configuration>
    </node>
</vagus>
